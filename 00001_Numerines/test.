test "filter" {
  assert.that(
    numerines.valor().filter { n => n.between(7,17) } ==
    resultados.filter()
  )
}

test "any" {
  assert.that(
    numerines.valor().any { n => n.between(7,17) } == 
    resultados.any()
  )
}